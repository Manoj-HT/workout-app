import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.magibuilds.workoutapp',
  appName: 'workout-app',
  webDir: 'dist/workout-app/browser'
};

export default config;
